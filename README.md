# WebDAV Monitor

一款专为 **WebDAV** 与 **Alist** 设计的定时同步辅助工具，解决生成strm文件自动刷新与本地文件夹同步问题，配备高颜值 Analytics 风格 Web 管理界面，支持多账户、多语言，并具备高效的局部刷新逻辑。

## 🎨 UI 更新 (v2.0 — Analytics Theme)

本次 UI 全面升级为 **Analytics Dashboard** 风格：

- **深色主题**：全局 `#111111` 极夜黑背景，`#1C1C24` 卡片层
- **Dashboard 看板**：统计卡片（总任务 / 运行中 / 账号数）+ 柱状图 + 环形图
- **模块化布局**：WebDAV 与 Alist 严格分离，各自独立管理页面
- **侧边栏**：图标导航 + 激活状态指示点，分组展示 Modules / System
- **顶部栏**：面包屑导航 + 搜索 + 通知 + 语言切换 + 用户头像
- **弹窗 & 表单**：统一 `#1C1C24` 背景，`#111111` 输入框，圆角卡片风格
- **默认语言**：中文

## 🌟 核心特性

- **🎨 Analytics UI**：现代深色 Dashboard 风格，交互优雅，丝滑流畅
- **🌐 国际化支持**：完美支持 **简体中文** 与 **English**，默认中文
- **🔐 安全加固**：集成 **JWT 认证** 登录保护，支持通过环境变量自定义密钥
- **🚀 局部精准刷新**：智能对比文件变动，仅刷新 Alist 对应的子目录，极致节省资源
- **📊 任务看板**：实时查看任务运行状态、成功/失败计数及历史记录
- **🧪 连接测试**：内置 WebDAV 和 Alist 账号连通性检测，配置即时反馈

## 🚀 快速开始

### 使用 Docker Run

```bash
docker run -d \
  --name webdav-monitor \
  -p 8000:8000 \
  -e JWT_SECRET=your-secure-secret-key \
  -v $(pwd)/data:/app/data \
  liheng6668/webdav-monitor:latest
```

### 使用 Docker Compose

```yaml
version: '3.8'
services:
  webdav-monitor:
    image: liheng6668/webdav-monitor:latest
    container_name: webdav-monitor
    restart: always
    ports:
      - "8000:8000"
    environment:
      - JWT_SECRET=your-random-secret-key
    volumes:
      - ./data:/app/data
```

### Multi-Platform Build（开发者）

```bash
docker buildx build --platform linux/amd64,linux/arm64 -t liheng6668/webdav-monitor:latest --push .
```

## 📖 使用指南

1. **登录控制台**：访问 `http://IP:8000`，默认账号 `admin` / 密码 `admin`（请登录后立即修改）
2. **配置账号**：在 WebDAV Accounts / Alist Accounts 中分别添加对应账号
3. **建立任务**：关联账号并设置监控路径与扫描频率
4. **系统设置**：可随时切换界面语言（中/英）或修改登录密码

## 🛠️ 技术选型

- **Frontend**: Vue 3 + Vite + Tailwind CSS + Lucide Icons
- **Backend**: FastAPI + APScheduler + PyJWT
- **Database**: JSON File Persistence (`data/config.json`)

## ⚖️ 许可

MIT License
